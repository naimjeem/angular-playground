import { BtnFormatDirective } from './btn-format.directive';

describe('BtnFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnFormatDirective();
    expect(directive).toBeTruthy();
  });
});
